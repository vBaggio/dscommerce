spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dscommerce
    username: root
    password: dbpass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

